<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
</head>
<body>
    <h1>Text template</h1>

    {% include 'nav.twig' %}

    <ul>
        {% for array in arrays %}
            <li>{{ array.id }} - {{ array.name }} - {{ array.office }}</li>
        {% endfor %}
    </ul>

    {% for i in 0..10 %}
        * {{ i }}
    {% endfor %}
    <br>
    {% for letter in 'a'|upper..'z'|upper %}
        * {{ letter }}
    {% endfor %}
    <br>
    {{ 1 + 2 }}


</body>
</html>